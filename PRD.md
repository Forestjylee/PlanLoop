# PlanLoop PRD（MVP）

> 状态：草稿待评审 ｜ 关联文档：[CONTEXT.md](CONTEXT.md)（领域术语）、[PROCESS_LOG.md](PROCESS_LOG.md)（决策过程）
> 交互原型参考：`planloop.html`（一次性构建的可点击原型，仅作理解参照，不代表最终实现）

---

## 1 需求背景

### 1.1 它要解决的问题

PlanLoop 服务一类特定人群：刚从职场脱离的自由职业者 / 数字游民 / 独立开发者。他们的共同状态是时间 100% 自主、外部约束为零——没有老板、没有固定会议、没有同事催促。

这个状态有一个鲜明代价：**启动困难**。面对空白的一天不知道该干什么，容易把黄金时段（如 10:00–12:00）耗在手机上，直到中午才进入状态。根本原因是"决定今天干嘛"这件事的开销过高，且每一天都要重新做一次。[PM judgment]

已有的待办清单工具对此无效——它们只帮你列出该做什么，不帮你在时间上"什么时候做"，等于把最难的结构化决策留给了意志力最低的时刻。

### 1.2 产品思路

把一个把"决策"从"实时做"前移到"提前做"。核心机制是：

- **时间块结构**固定不变，每天到了某个小时就自动进入某个状态（10 点就是深度工作）；
- **每日填充内容**在前一晚做好，执行时只走流程，不再现场决定；
- **执行记录 + 复盘**把"计划 vs 实际"的偏差变成下一次决策的输入。

数据全程存在本地，不注册、不联网，启动即用。

### 1.3 需求起源

用户本人正处在这个人群，本人即典型用户，痛点是亲身验证过的。产品做出来先给自己用，再谈其他。[PM judgment]

---

## 2 目标

定性目标，面向本人自用：

- 早上的"启动摩擦"下降：打开应用看到的是已就位的时间结构，而非空白一天。
- 黄金时段被结构性保护：默认进入深度工作块，而非被手机消耗。
- 形成可追溯的"计划 vs 实际"记录，让每天结束时有据可复盘。
- 全流程本地运行：无账号、无网络依赖、数据可导出备份。

---

## 3 术语与领域不变量

完整术语见 [CONTEXT.md](CONTEXT.md)。关键不变量（实现时必须守住）：

1. **只影响未来**：结构增删改只影响尚未定形的未来日子，永不追溯改写已产出的计划与执行记录。
2. **每日快照解耦**：一天定形时把结构字段复制进当日，该日自包含、与模板无引用关系。
3. **惰性物化**：一天仅当"成为今天"或"首次写入内容"时定形快照；其余未来日实时反映结构。

---

## 4 功能需求详述

### 4.1 时间块结构管理

#### 页面组成

结构管理页列出全部时间块：色标、名称、起止时间、重复规则。支持新建、编辑、删除。新建/编辑弹窗含：名称、开始、结束、颜色、重复规则（每天 / 工作日 / 周末 / 自定义星期）。

#### 业务逻辑

用户新增一个时间块后，它成为"未来尚未定形日子"的种子：从今天起所有充分未来的日子，只要符合该块的重复规则，就会获得这个时间块。按不变量，改成结构不影响任何已定形的日子——如果用户提前规划过某天，或某天已成今天，那么它保持自己的快照，不因这次增删改而变化。

删除一个时间块同理：只影响未来新定形的日子；过去已定形的日子保留原样。颜色用于区分块类型，与执行状态（是否完成）无关。

边界与例外：起止时间需校验（结束不早于开始，可跨零点即结束时间小于开始时间视为次日凌晨）。空名称不允许保存。删除需二次确认，且须在删除前明确提示"只影响未来日子，过去计划不受影响"。

#### 字段规则

| 字段 | 类型 | 必填 | 约束/范围 | 默认 |
|---|---|---|---|---|
| 名称 | 文本 | 是 | 1–30 字符 | 无 |
| 开始 | 时间 | 是 | 00:00–23:59 | 无 |
| 结束 | 时间 | 是 | 可跨零点 | 无 |
| 颜色 | 枚举 | 否 | 预设色板 8 色 | 取色板首色 |
| 重复 | 枚举 | 是 | daily/weekday/weekend/custom | custom(工作日) |

### 4.2 今日视图（每日计划 + 执行）

#### 页面组成

今日视图是每天的操作主界面：顶部是日期导航与"今天/周/复盘/模板"四类页签入口；右侧是当日概览（时间块数、已完成、已填内容）。主体为时间轴：按起止时间从上到下排列当天所有时间块卡片，每张卡片包含色条、模板名、起止时间、一个可点的状态徽章、一个任务名输入框、预估分钟数与"详情"展开区。

#### 业务逻辑

打开应用默认落在今天。今天若尚未定形，则按当前结构物化出当天的时间块（惰性物化不变量）。用户在卡片里填入"今天这个时间块要做什么"，保存后写入当日计划。

每个块的完成状态有四种：未开始 / 已完成 / 部分完成 / 跳过。用户可直接点卡片上的状态徽章循环切换（未开始→已完成→部分完成→跳过→未开始），实现"按计划完成"的一键标记；也可在"详情"里选状态、填实际开始/结束时间、备注与实际备注。

执行记录只记状态与用户填写的字段，不做自动计时（计时器不在 MVP 内）。

边界与例外：当天结束后（进入复盘），今日视图侧重浏览；任务名输入无长度硬限制但应有合理上限；默认给用户"未完成可带入待办池"的出口（见 4.4）。

### 4.3 复盘（计划 vs 实际）

#### 页面组成

复盘页面向"过去的日子"和"今天的结束"：列出所选日期的每个时间块，逐行显示"当天填的任务 + 该块当时安排的时间"对比其执行状态，每行带一个"放进待办池"的勾选框。页面下方是今日心情（5 档）和一段复盘文本框。

#### 业务逻辑

用户在一天结束或次日早晨进入复盘。系统把当天计划（任务与预估）与实际执行（状态、实际备注）并列展示，形成偏差视图。用户可勾选某块"未完成/待续 → 放进待办池"，携带其任务内容与来源（原日期/原块）。

"把打勾的放进待办池"将所选任务的完整内容（任务名、备注、预估）生成一个待办池条目，并把源块标记为已处理。心情与复盘文字按日期保存，属该日执行数据，不因结构改动而丢失（不变量：只影响未来）。

### 4.4 遗留待办池

#### 页面组成

待办池是一个跨日未决任务的收容器。列表展示每条：来源（日期/时间块）、任务名、预估时长，以及一个"分配到时间块"的操作与删除操作。它有独立页签入口，也可在未来某天的计划页内打开调用。

#### 业务逻辑

这是"带到明天"的落点。未完成任务被用户放进待办池后，它脱离原来那天，成为跨日待办。次日（或任意未来某天）做计划时，用户从待办池取一条，"分配"进当天某个时间块，写入该日计划；分配后条目标记为已处理（不再出现在待取用列表，除非再次放进）。

MVP 内分配为手动完成——用户选定目标日期与目标块。不做自动建议或自动排期。

边界与例外：待办池条目本身不参与执行状态（它是计划源的暂存），执行状态只存在于它被分配进去的那几天里。空任务名不允许放入待办池。

### 4.5 周视图

#### 页面组成

周视图以周一为起点展示连续 7 天，每天一列。每列顶部是日期，列内罗列该日的时间块，每块显示色标、模板名、完成状态图标。点任意一块可跳转到对应日期的今日视图。

#### 业务逻辑

周视图是概览用途：一眼看出每天结构长什么样、每个块的完成状态分布。它遵守惰性物化：未定形的未来日显示的是结构的实时投影（取决于"周起始基准日"，跨周通过前后翻页浏览）。

### 4.6 数据导出 / 导入

#### 页面组成

一个导出入口（下载 JSON）与一个导入入口（选择 JSON 文件）。

#### 业务逻辑

导出把全部结构、已定形各日计划与执行记录、待办池整体序列化为一个符合本项目数据结构的 JSON 文件供下载，用于备份与换机。导入读取 JSON 并在确认后覆盖本地数据（导入前应有"将覆盖当前数据"的明确提示）。这是本地应用的兜底能力，因无云端同步，导出/导入是数据得以存续的唯一通道。

### 4.7 主题切换与基础框架

支持浅色/深色两套主题，选择持久化到本地。数据统一存在本地存储，所有写操作即时持久化。

### 4.8 新手引导（Onboarding）【CR-001】

#### 页面组成

一种覆盖层式分步高亮导览（Tour）。它以半透明遮罩 + 焦点高亮 + 气泡的形式，依次指向 5 个核心功能入口，每个气泡说明该区域作用。顶部栏常驻一个「？」按钮作为手动重开入口。

#### 触发与生命周期

- **首次启动自动触发**：用户第一次打开应用时（此时尚未看过导览），自动从头开始播放一次。播放前先切换到对应页签使高亮目标可见。
- **完成判定**：无论是否走完所有节点，只要导览被关闭（点外、按 Esc、点「跳过所有」或走到最后一步），即标记为「已看过」，此后启动不再自动弹出。
- **手动重开**：点击顶部「？」按钮从头播放一次；重播不改变「已看过」状态（启动仍不自动弹）。
- **状态持久化**：onboarded 标记随其余设置存入本地，随导出/导入迁移。

#### 导览节点（5 个，顺序固定）

| # | 目标区域 | 高亮 | 气泡文案要点 |
|---|---|---|---|
| 1 | 顶部页签「今日」 | 高亮今日页签 | 这就是你的每日主界面，打开应用默认落在这里 |
| 2 | 今日卡片状态徽章 | 高亮一个状态徽章 | 点击可循环切换 未开始/已完成/部分完成/跳过，一键标记执行情况 |
| 3 | 顶部页签「复盘」 | 高亮复盘页签 | 每天结束来这里做「计划 vs 实际」对比，未完成的放进待办池 |
| 4 | 顶部页签「待办池」 | 高亮待办池页签 | 跨日未决任务在这里暂存，次日做计划时取用分配进时间块 |
| 5 | 顶部页签「模板」 | 高亮模板页签 | 时间块结构在这里一次搭好，之后每天只往填内容 |

#### 交互规则

- 气泡提供「上一步 / 下一步 / 跳过所有」三个操作；下一步走到最后一个节点时变为「完成」。
- 点击气泡外部、或按 Esc、或点「跳过所有」，立即关闭本次导览并标记已看过。
- 气泡优先出现在高亮目标的旁侧（上/下/左/右就近避开边缘）；窗口过小时允许气泡缩小或滚动到目标可见。
- 导览为纯展示层，不阻塞底层操作之外的功能，也不改写任何业务数据（不含页面切换带来的惰性物化副作用）。

#### 边界与例外

- 若目标区域在切换页签后仍不可见（极端窗口），仍照常显示气泡并在文案中给出文字指引兜底。
- 导览不参与数据导出内容本身，但 onboarded 标记属于设置项，随设置导出。

### 4.9 今日时间轴视图与拖拽调整（CR-003）

#### 页面组成

今日（单日）视图的主体从"垂直卡片列表"改为**日历式纵向时间轴**：左侧为从 0:00 到 24:00 的整点刻度线；时间块卡片按起止时间做绝对定位——**卡片顶端 = 开始时间、卡片高度 = 时段长度（按分钟比例）**，从而在轴上"盖上"这一段时间。卡片内部不再显示起止时间文本（时间由轴上位置与高度表达），保留色条、模块名、状态徽章、任务名输入框与详情展开。

#### 业务逻辑

用户可**上下拖动卡片改变时段**：以按住拖动的净位移换算成分钟，平移该块在当日的起止时间（**时长不变**）。松开后生效并持久化。块的先后顺序随之改变。

- **作用域**：仅修改该块在"当前查看日"这一日的时段，**不回写模板**。按领域不变量，这属于当日快照的更新，不影响已定形的其他日子、也不影响模板这个未来种子。
- **辅助对齐**：拖动时**实时吸附到整点（每小时一个槽位）**，拖动中在卡片右上方显示当前吸附到的整点时间标签，释放后落定。

#### 边界与例外

- **跨零点块**（结束 ≤ 开始、语义上跨到次日凌晨）：本日时间轴只绘制"0:00–24:00"内的可见段。
- **末端越界**：拖动后的开始时间限制在 `0:00` 到 `1440−时长` 分钟之间，不足整块时长的尾段会被夹持，不越出当日。
- **重叠**：两个块时段重叠时允许并存，MVP 不做自动避让堆叠。
- **范围**：时间轴仅用于"今日/单日"视图；本周视图、复盘、待办池、模板页保持现有形态不变。

### 4.10 产品理念开场引导（CR-009）

#### 页面组成

首次使用在进入既有 5 步操作导览之前，先播放一屏「产品理念」开场：以 2–3 张卡片顺序讲述 PlanLoop 的核心设计哲学，用最口语的话让用户第一次打开就理解"为什么这样设计"，而不只是"按钮在哪"。此段与既有导览构成**两段式首次引导**：理念开场 → 操作导览。

#### 理念卡片（顺序固定，2–3 张）

| # | 标题 | 一句话要点 |
|---|---|---|
| 1 | 把决定提前 | "今天干嘛"是最耗意志力的决定，别等早晨现想——用固定时间块结构提前排好，把意志力留给执行。 |
| 2 | 结构护住黄金时段 | 10 点默认就是深度工作，结构性挡住刷手机；休息就是休息，不必自责。 |
| 3 | 只影响未来 | 改结构只影响还没定形的日子，已经记录的过去不会被推翻——放心试，错了明天再调。 |

#### 业务逻辑

- **触发**：首次打开（尚未看过理念开场，`settings.philoIntro` 为空）且尚未看过操作导览时自动播放。
- **完成判定**：任意一步点「开始使用」/看完全部卡片即视为已看过（置 `philoIntro`），不再自动弹。
- **与操作导览衔接**：看完整段理念后，若 `onboarded` 仍为空，紧接启动既有 5 步操作导览；若已看过操作导览（如老用户重开），则本次只播理念段。
- **手动重开**：顶栏「？」按钮保留，仍重播既有操作导览，不重播理念段。
- **纯展示**：理念段不触碰任何业务数据，仅一个设置标记，领域接口不变。

### 4.11 心情 3 档 + 富文本复盘（CR-010）

#### 心情粒度调整

复盘页心情由 **5 档改为 3 档（好 / 中 / 坏）**，匹配"每天自我感觉"的轻量记录心智，降低每天打卡成本。

- 三档对应 `mood = 1 / 2 / 3`（1=坏，2=中，3=好）；未选为 `0`。
- **兼容**：既有 5 档存档自动归并——`4/5 → 3（好）`，`3 → 2（中）`，`1/2 → 1（坏）`。

#### 富文本复盘（可选）

复盘文字默认仍是普通输入，但支持**开启 Markdown 语法**，让长篇复盘有层级（加粗、列表、标题）。存储仍为**纯 Markdown 源码字符串**（保持单文件、导出干净），渲染时解析。

- **能力范围（Markdown 子集）**：标题（`#`–`###`）、加粗、行内代码、无序列表、有序列表、空行分段。不做图片/链接/表格/代码块等重能力。
- **输入**：复盘文本框保持 `textarea`，用户直接键入 Markdown 标记。
- **预览**：文本框下方提供「预览」切换，把当前文本用 `mdToHtml` 解析渲染供查看；切回继续编辑。
- **安全**：解析层对全部用户文本先 `esc` 转义再套结构标签，杜绝 XSS。

---

## 5 范围边界（MVP 不做）

明确排除，留作后续：自动优化算法（AI）、番茄钟/计时器、云同步/多端、移动端、日历集成（CalDAV/Google）、团队协作、历史统计图表。虽然这些在后续迭代路线里很诱人，但 MVP 的核心是把手动闭环跑顺。

---

## 6 核心用户流程

每天一次完整闭环：

```
前一天晚上
  打开模板已就位的"明天" → 从待办池取未完成任务分配进块，或直接填新任务
  →
当天
  早上打开 → 今天结构已物化 → 直接按块执行，点徽章标记完成状态
  →
当晚或次日早
  复盘：看"计划 vs 实际"，勾选未完成项 → 放入待办池 → 填心情与复盘
  →
回到"前一天晚上"，循环
```

可用性钩子：黄金深度工作块默认落在 10:00–12:00，保护最容易浪费的时段；结构由用户一次搭好，之后每天只填内容。

---

## 7 MVP 完成判据

- 打通上述闭环：搭结构 → 填今天 → 标记执行 → 复盘放入待办池 → 明天取用，全流程可操作。
- 三条领域不变量（只影响未来 / 每日快照解耦 / 惰性物化）在数据层成立，并有测试兜底。
- 数据可导出、可导入，离线可用，无账号。

---

# 变更 · 轻量人生节奏转型（CR-015）

> 状态：grill 已收敛，本文为转向后的目标形态。**本章节取代上文 §1–§7 中以"硬时间块承诺"为前提的内容**；§4 的 CR-003 时间轴拖拽、§4.4 遗留待办池、部分 §4.10 理念卡与本方向冲突，按本章节弃用。旧文本保留作历史存档，由本变更锚定一致性。
> 领域术语以重写后的 [CONTEXT.md](CONTEXT.md) 为准。

## 8.0 为什么转向

原设计把"时间块"当**纪律与承诺**（定了就必须做、计划 vs 实际 复盘纠错）。用户痛点聚焦后确认：伤害内耗的正是"承诺成本"——每份计划都在制造必达承诺，现实稍有偏差承诺就变成自责素材。用户明确表示"过度规划会加重内耗，但完全不规划也不行"。故转向：

> **减内耗的关键 = 把"承诺"做轻、把"结构"做稳。** 结构保留(差异化不丢)，但由**纪律**软化成本**节律**；日常负担的主战场 = **"填计划本身费劲"(操作负担)**，故产品重心转向"让填变成点选"。

## 8.1 产品定位（最终）

「**轻量人生节奏工具**」——介于"大而全人生规划器"与"极简自由清单"之间：
- 保留**时段结构**(自然时段)，让人"醒来即知今天大形"，但不逼守点；
- 用**候选人选式填充(填=选)** 消灭操作负担；
- 以**无咎顺延 + 回望沉淀**消化内耗，不做偏差纠错。

## 8.2 grill 收敛决策（Q1–Q9）

| # | 决策 |
|---|---|
| Q1 | 首要痛点是**操作负担**；设计口号「填=选，不是写」 |
| Q2 | **复盘弱化为「回望」**：只保留完成态 + 正向沉淀(连续天数/累计/趋势)，**移除"计划 vs 实际"偏差问责** |
| Q3 | 未完成 → **一键顺延，可换时段**，过程中零必填、零问责 |
| Q4 | **取消"精确到钟点的时间块"，改自然时段**(上午/午间/下午/晚上/午夜) |
| Q5 | 时段**带可改默认钟点窗口**(仅弱提示当前段，不逼守点)；调整时段=只改顺序，不做精确拖时刻 |
| Q6 | 候选来源：**昨日+高频复用 ＋ 固定钉**并重 |
| Q7 | **低频优先去重**：钉的恒排最前 → 最近/高频紧随；同内容去重只占一格 |
| Q8 | **遗留待办池并入候选池**：顺延项自然成为未来某时段候选之一，不设独立概念 |
| Q9 | **只保留「只影响未来」；砍掉「每日快照/惰性物化」**：每天实时反映当前结构，过去只留"当时填了啥" |

## 8.3 目标（面向自用）

- 打开即见当天的**时段结构**与**可点选候选**，而非空白或必填表单。
- "填"一个时段 ≤ 1~2 次点选，几乎不用思考、不用打字。
- 没做完的时段，一次轻点即可无咎顺延；不弹任何"连续未完成"问责。
- 每天结束时回望：完成状态 + 正向沉淀；记录产生复利，不制造内耗。
- 全流程本地运行、无账号、可导出备份。（沿用）

## 8.4 功能需求（转向后）

### 8.4.1 时段结构管理
预设 上午/午间/下午/晚上/午夜 五个自然时段；支持改名、增删、排序。每个时段可选设一个**默认钟点窗口**(如 上午≈06–12)，仅作"当前该看哪段"的弱提示。**不变量**：结构改动只影响未来，过去只留记录。

### 8.4.2 今日视图（节律填充 + 执行）
主体为**自然时段列表**，每行一个时段；该时段显示今日倾向内容(可空) + 完成/未完成二态。点击某时段弹出/内联展示该时段的**候选池**，点选一项即填入(填=选)；候选池含固定钉 + 最近/高频 + 顺延项(低频优先去重)。执行时点时段上的完成钮即可标记/撤销。

### 8.4.3 回望（取代"复盘"）
不显示"计划 vs 实际"对比。展示当日完成态 + 正向沉淀(连续天数/累计/周趋势——复用 CR-014「本周你在变好」沉淀区) + **心情 3 档(好/中/坏)** + 一句话记录(Markdown 可选)。纯记录与回望，无偏差纠错。

### 8.4.4 一键顺延（取代"带到明天/待办池"）
某时段未完成，点"顺延"即把该项变为**未来某天某时段的候选之一**；默认顺到同类时段，但可换到别的时段。不设独立待办池页签，顺延项只出现在目标时段的候选池里。

### 8.4.5 周视图
保留概览(7 天 × 时段完成态)，并入回望沉淀区(趋势/完成率)。不依赖精确钟点定位。

### 8.4.6 数据导出/导入、主题、新手引导
沿用；新手引导/理念卡文案需从"把决定提前/结构护住黄金时段/只影响未来"调整为"节律、填=选、无咎顺延、只影响未来"的转向后表述。

## 8.5 范围边界（转向后仍不做）

自动优化/AI、计时器、云同步/多端、移动端、日历集成、团队协作、历史统计图表(仅正向沉淀)、**精确钟点的时间轴与拖拽**(CR-003/005/006 弃用)、**独立遗留待办池**。

## 8.6 转向后 MVP 完成判据

- 完成一次节律闭环：见时段结构 → 点选候选填今日 → 执行标记完成/撤销 → 一键顺延到未来某时段 → 回望看正向沉淀。全流程可操作。
- 「只影响未来」不变量在数据层成立，并有测试兜底。
- 精确钟点时间轴、拖拽、独立待办池等废弃面已从代码与文案中移出，不留死代码。
- 数据可导出/导入，离线可用，无账号。