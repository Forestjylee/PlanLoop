# PlanLoop 过程日志 (PROCESS LOG)

> 用途：记录 PlanLoop 从需求到实现的每一步决策与理由。**所有后续变更（修 bug、新需求、重构）都必须在此追加记录，且遵循 project_memory.md 中的立项流程。**

## 流程规约
1. 需求严审 (grill-with-docs) → 2. PRD → 3. Spec → 4. 实现 → 5. 回写日志
每次变更闭环后，在此新增一节。

---

## 阶段 0：立项前（已发生，作为背景记录）

- **2026-08-21** 构建了一个一次性可用原型 `planloop.html`（单文件本地 Web 应用，localStorage 存储，含时间块骨架/每日填充/一键执行状态/复盘带到明天/周视图/模板管理/深色主题/导出）。
- **2026-08-21** 用户决定**暂停直接开发**，改为先走完整立项流程：grill 需求 → PRD → Spec → 再写代码，全程留日志。既有 `planloop.html` 降级为「待审原型参考」，不代表最终实现。

---

## 阶段 1：需求严审（grill-with-docs）

> 逐项逼问中。每解决一项在此记录「问题 → 决策 → 理由」。

### 已解决决策

**Q1：骨架"固定"的确切含义**（2026-08-21）
- 决策：**骨架可随时微调，且只影响未来日子；永不追溯改写已产出的计划与执行记录。不引入"执行期"概念。**
- 理由：完全固化的"执行期"哲学上最纯粹，但会显著降低自由职业者临时调整的可用性；纯当灵活模板又等于放弃差异化。折中方案"快照+只影响未来"兼顾哲学与可用性，且实现简单。
- 领域不变量已写入 CONTEXT.md。

**Q2：每日计划与骨架的关联方式**（2026-08-21）
- 决策：**每日快照解耦**——当天首先生成时把骨架字段复制进当日,该日自包含、与模板无引用关系；模板只作为"未来全新一天"的种子。
- 理由：草案的"templateId 实时引用"违背 Q1 不变量(删模板会追溯破坏过去)。快照解耦与 Q1 完全一致、实现最干净，也让"骨架变了旧日闪过"天然成立。

**Q3："带到明天" (Carry) 的判定与去向**（2026-08-21）
- 决策：**落到"遗留待办池 (Backlog)"**——把未完成/待续内容送入跨日待办池,标识其来源(原日期/原块),次日由用户手动分配进具体时间块。**不自动落到特定时间块。**
- 理由：用户否决了"状态判定+同名块自动继承"。待办池更灵活(可换块/换日),把"要不要续"和"续到哪"的决策都留给用户,也更契合"把决策提前做但不替他决定"的产品哲学。代价是 MVP 需额外支持一个待办池的收容/取用界面。

**Q4：一天"定形/物化"的时机与复盘边界**（2026-08-21）
- 决策：**惰性物化(Lazy Materialization)**——一天仅当"成为今天"或"首次被写入内容"时快照定形；其余未来日不固化、实时反映骨架。复盘面向过去天与今天。
- 理由：促成"今天改骨架 → 所有未定形未来日跟进"的直觉语义(真正实现"只影响未来")；又因为"首次写入才定形",允许用户提前数天规划而不会意外被骨架改动覆盖。若提前N天预物化会削弱 Q1。

**Q5：MVP 范围边界**（2026-08-21）
- 决策：**维持草案排除清单**(计时器/移动/云同步/日历集成/AI/协作/统计统统不进 MVP)；将**待办池、数据导出+导入、周视图**纳入 MVP。
- 理由：待办池是 Q3 定下的核心闭环一环,导出/导入是自用产品防丢与换机的刚需,周视图代价低且是概览基本盘。

### 严审结论（grill 收敛）
以上 5 项即为进入 PRD 的承重决策。其余(执行状态录入方式、待办池→明日的取用交互、复盘触发方式等)归入 Spec 阶段细化。

## 阶段 2：PRD

- **2026-08-21** 基于 grill 5 项决策撰写 `PRD.md`（常规章节式，交互原型引用 `planloop.html`）。
- **2026-08-21** PRD 经用户评审通过。

## 阶段 3：Spec

- **2026-08-21** 开始把 PRD 转为技术规格与任务清单 `SPEC.md`。
- **2026-08-21** `SPEC.md` 完成：单文件架构、领域纯函数层（可测）+ DOM 胶水层、7 个 TDD 任务。三条领域不变量由"仅已物化日子入库"的命中集合直接保证，并有测试兜底。

## 阶段 4：实现（MVP）

- **2026-08-21** 子代理驱动方式执行：Agent A 实现领域纯函数层 + TDD 测试（`node tests/domain.test.js` → PASS 45/0）；Agent B 实现 DOM 胶水层 + README。
- **2026-08-21** 结果：`planloop.html`（单文件，5 页签：今日/本周/复盘/待办池/模板，含状态徽章循环、惰性物化、待办池分配、导入导出、深浅主题、首用种子骨架）；`tests/domain.test.js` 全绿；脚本语法校验通过；静态审查无未定义引用。
- **遗留验证**：本环境 headless Edge 的 `--dump-dom` 未产生输出，运行态最终需用户本地打开确认一次。已知取舍：a) 打开某未来日视图会物化该日（调度面，周视图保持纯投影）；b) `carrySent` 已置位但复盘未据此禁用重复入池（可能产生重复条目）。两者为 MVP 可接受取舍，待后续变更可收紧。

## 变更请求 CR-001：新手引导（Onboarding）

- **2026-08-21** 用户提出：首次使用默认自动开始引导介绍功能用法；用户后续可手动重新开启引导。
- **grill 已收敛，四项决策**：
  - **D1 形态**：分步高亮导览（5 节点：今日 → 状态徽章 → 复盘 → 待办池 → 模板）。
  - **D2 触发**：首次启动自动弹出；顶部常驻「？」按钮可随时手动重开，重播从头开始。
  - **D3 完成判定**：关闭气泡即视为已看过（onboarded），只自动弹一次；手动重播不改变该状态。
  - **D4 交互**：气泡含「上一步 / 下一步 / 跳过所有」，点击气泡外部或按 Esc 关闭；气泡出现在高亮目标旁最近可用位置。
- **流程状态**：grill 完成，进入 PRD 撰写（新增 PRD 章节 4.8 新手引导）。
- **2026-08-21** PRD 章节 4.8 经用户评审通过。进入 Spec（新增 SPEC 章节 6「CR-001 实现规格」）。
- **2026-08-21** SPEC 章节 6 完成（纯胶水层 + `settings.onboarded`，不触碰领域接口，domain 测试保持全绿）。进入实现。
- **2026-08-21** CR-001 实现完成：`planloop.html` 新增顶部「？」按钮、`tour-veil/tour-hl/tour-bubble` 三覆盖层、5 节点导览引擎（首开自动 + 手动重开、上一步/下一步/跳过/点外/Esc 关闭、关闭即置 `settings.onboarded`、scroll/resize 重定位、无徽章时回退 `#daySummary`）；`README.md` 补充引导说明。`tests/domain.test.js` 保持全绿（45/0），脚本 `node --check` 通过。
- **遗留验证**：与 MVP 相同，本环境 headless 无法跑运行态，导览实际弹窗/位置/关闭行为需用户本地打开浏览器确认一次（清 localStorage → 首开自动弹、关闭后刷新不再弹、点「？」重播且刷新不自动弹）。

## 变更请求 CR-002：导览气泡 UI 修复 + 术语「结构」更名

- **2026-08-21** 用户反馈两点：a) 引导气泡 UI 显示怪异，出现「连线歪到页面左侧」；b)「骨架」一词听起来不自然，要求更换。
- **诊断**：用 Chrome 无头截图复现。气泡定位、高亮框对准都正确；「歪线」来自**手写 `clip-path: polygon` 遮罩**在目标靠近视口边缘/洞尺寸变化时产生的一条长斜边；且气泡缺指向箭头、与目标仅靠位置暗示关联，被用户感知为"一根线歪了"。
- **grill 决策**：
  - **D1（UI）**：改用 `clip-path: inset(…)` 挖洞，消除斜线毛刺；气泡紧贴目标（gap 8px）并在指向目标一侧加 CSS 三角箭头（动态精确对准目标中心，含滚动/缩放重算与窄视口兜底）。
  - **D2（术语）**：将「时间块骨架 Skeleton」更名为 **「时间块结构」**（保留英文关键词 Skeleton；不再用"骨架"）。影响面：应用内全部用户可见文案 + `PRD.md / CONTEXT.md / SPEC.md / README.md` 四个活性文档。
- **实现**（`planloop.html` DOM 胶水层 + 文档）：新增 `.tour-bubble .arr` 箭头 CSS、改为 `inset()` 遮罩、`renderTour` 注入箭头元素；全局替换术语。`tests/domain.test.js` 保持全绿（45/0），`node --check` 通过；headless 渲染确认箭头尖端正对目标中心、`inset` 无斜线、气泡不越界。
- **变更闭环**：grill→（PRD/SPEC 术语替换）→实现→日志。历史 PROCESS_LOG 记录保留原「骨架」字样，由本条目与其"更名"决策锚定一致性。

## 变更请求 CR-003：今日时间轴视图 + 拖拽调整时段

- **2026-08-21** 用户新需求：a) 时间块可拖动改变先后顺序；b) 今日改为"左侧 0–24 点时间轴，卡片不显示时间、高度覆盖活动延续时间"。
- **grill**：两条需求在加轴后高度耦合——轴上的上下位置即代表开始时间，故「拖动改顺序」收敛为**拖动=平移时段（时长不变、先后顺序随之变）**；范围限定**仅作用于当日快照、不回写模板**（符合"只影响未来/每日快照解耦"不变量）。用户跳过问答、采用推荐项。
- **PRD §4.9**：新增今日时间轴视图与拖拽小节，明确页面组成、业务逻辑、边界（跨零点只画当日段、末端夹持、重叠允许、仅限今日视图）。
- **Spec §8**：领域层新增可测纯函数 `clampStart`（+`hmToMin`/`minToHM`），其余为 DOM 胶水层渲染+交互改造。
- **实现**（`planloop.html`）：
  - 今日 `#timeline` 改为相对定位、高 `1440×PPM(0.72)`；左侧 `.gutter` 每小时刻度（0…24）。
  - 卡片 `.block` 绝对定位：`top=开始分钟×PPM`、`height=时段分钟×PPM`；**移除卡内起止时间文本**；展开详情时 `height:auto` 向下生长。
  - 拖拽：`mousedown` 于卡片标题/空白区（跳过徽章与「详情」），全屏 `mousemove` 平移，`clampStart` 夹在 `[0,1440−dur]`、吸附 5 分钟；`mouseup` 以 `start/end=start+dur` 写回当日并 `persist()`。
  - 进入今日自动滚动到"当前时刻−35%视口"处（一次，切页重置）。
- **测试**：领域层新增 `clampStart` 6 例 → `tests/domain.test.js` **PASS 51 / FAIL 0**；`node --check` 通过；headless 截图确认左侧时间轴、卡片按时段绝对定位、卡内无时间文本。
- **遗留验证**（同前，需用户本地打开确认）：拖拽手感与吸附、展开详情时卡片是否遮挡下方块、跨零点与极端窄窗口表现。MVP 取舍：重叠块不做避让堆叠。

**CR-003 追加（同日用户反馈"UI 好丑 + 每小时该是槽位"）**：
- **吸附粒度**：由 5 分钟改为**整点（每小时一个槽位）**——`Math.round(delta/60)*60`；拖动实时吸附，卡右上方以 `--t` 变量经 `::after` 显示当前吸附到的整点时间标签。
- **UI 视觉升级**：① 时间轴背景以 `repeating-linear-gradient` 叠加每小时交替浅色 band（槽位感）；② 卡片色条由顶部 4px 横条改为**左侧 5px 竖向色条**（圆角内嵌）；③ 新增**当前时间红色竖线+圆点**（仅今日视图）；④ 新一轮配色/字体/阴影（浅色 `#f5f6fa`、卡片圆角 14px、双层柔和阴影、标题字重 800）；⑤ 徽章/按钮/详情输入框圆角与 hover 态齐整。
- **校验**：`node --check` 通过，`tests/domain.test.js` 51/0 全绿，headless 截图确认槽位/色条/当前时间线均生效。PRD §4.9 与 SPEC §8 同步吸附粒度描述。

## 变更请求 CR-004：整体 UI/UX 视觉升级（苹果式设计系统）

- **2026-08-21** 用户反馈：现有 UI/UX 仍显原始、不够精美，要求模仿苹果及当下热门网站风格，优化整个页面。纯视觉诉求，不改变既有功能与领域逻辑。
- **grill 决策**：
  - **D1 设计基调**：苹果式克制设计语言——浅色 `#eef1f7` 背景+顶部径向渐变（`sidebar-light` 柔化）、卡片 `#fff`、深色 `#0b1220` 文字；大圆角 `--radius:18px`；双层柔和阴影（`.shadow` / `.shadow-hover`）区分静息与悬停。
  - **D2 材质与动效**：顶栏毛玻璃（`backdrop-filter:saturate(180%) blur(16px)` + 向下渐隐 mask）、页面切换淡入上移动画 `fadeUp`、模态弹入 `modalIn`、卡片悬停微缩放 + 阴影抬升、时间卡左 5px 竖向色条。
  - **D3 主题双轨**：深浅主题以 `data-theme` + CSS 变量双通道维护，保证两种主题下品牌色 `--accent:#0a84ff` / 状态色（完成绿/部分橙/跳过灰/危险红）对比度一致。
  - **D4 无障碍/微细节**：`-webkit-font-smoothing` 抗锯齿、`focus-visible` 焦点环、定制滚动条、斑马时间带 `repeating-linear-gradient`（承前 CR-003 槽位）。
- **实现**（`planloop.html` 样式层整体重写，函数逻辑与 DOM 结构零改动）：新增/重构 `:root` 设计令牌、`.topbar/.nav/.iconbtn/.card/.modal/.btn` 等全局组件样式、动效 keyframes、`[data-theme=dark]` 覆盖层。修复一处 `:root` 内 `--ok` 重复定义导致的样式异常。
- **校验**：`node --check` 通过；`tests/domain.test.js` **51/0** 全绿（领域层零触碰）；CDP 无头浏览器实机点击五页签（今日/本周/复盘/待办池/模板）+ 展开今日详情，逐页截图确认**无布局错乱、无元素溢出、无文字重叠**；浅/深双主题「今日」截图确认质感一致。核心交互语义（复盘勾选=未完成项入待办池、周视图当日列标「今天」、配色由用户模板决定）均保持不变，属既有行为而非回归。

## 变更请求 CR-005：短时段卡片自适应展开 + 拖拽/重叠动效强化

- **2026-08-21** 用户反馈两点：a) 时段较短的卡片信息会被边框遮挡显示不全；b) 动态效果不明显，要求强化拖拽动效与「两个卡片同一时段重叠」时的视觉/动画反馈。纯 UI 层改动，不触碰领域接口（domain 测试保持全绿）。
- **grill 决策**：
  - **D1（短卡自适应）**：卡片由「高度=时段」改为 `min-height = max(时段高度, MINFIT 100px)` 且高度自动流式铺开，保证即使 1 小（含 30 分钟）也能完整显示 标题/状态徽章/任务输入/预估。按「重叠允许」不变量，短卡可向下延展盖住下一槽位。
  - **D2（重叠判定）**：仅对**真实时间段交集**（分钟区间相交）判为重叠，不做像素级判断——避免因「短卡撑高」造成的误报。新增纯函数 `rangeOverlap(a,b)`（兼容 `'HH:MM'` 与数字分钟两种入参）。
  - **D3（静态重叠样式）**：重叠卡片加 `.clash` 警告边框 + 卡内 meta 显示 `⚠ 时段重叠：{伙伴名称}` 徽标（`clashblog`）。
  - **D4（拖拽动效）**：拖拽卡 `dragLift` 放大+微旋转+强调阴影；拖拽过程中实时计算临时时段，与之重叠的静态卡加 `drag-hit` 脉冲高亮，并在被拖卡底部弹 `clashtip` 浮动提示「⚠ 时段重叠：…」；卡片进场 `blockIn` 交错淡入。
- **实现**（`planloop.html` DOM 胶水层 + CSS，核心领域函数零改动）：新增 `BLOCK_MIN_PX`、`rangeOverlap`、`dayBlockEls` 映射；`renderDay` 计算 `clashMap` 并给重叠卡打标、`min-height` 自适应、进场交错延迟；`mousemove` 拖拽期间驱动 `drag-hit`/`clashtip`；CSS 新增 `@keyframes blockIn/dragLift/hitPulse/tipIn/chime` 及 `.clash/.drag-hit/.clashtip/.clashblog`。
- **校验**：`node --check` 通过；`tests/domain.test.js` **51/0** 全绿；CDP 无头浏览器渲染「今日」浅/深双主题截图，确认短卡（1h/30min）信息完整、重叠卡带警告边框与「时段重叠」文案；CDP 合成拖拽事件实机验证：把「运动」卡拖到 11:00 即刻出现 3 张 `drag-hit` 高亮与被拖卡浮动提示（修复了 `rangeOverlap` 误把数字分钟当小时解析的边界缺陷）。

## 变更请求 CR-006：拖拽体验优化（抓手/防错位/重叠避让）+ 日程模板预设

- **2026-08-21** 用户四点诉求：① 可拖拽区域缺乏明显标识；② 快速上下往返拖动后卡片与鼠标错位；③ 松手时若与其它卡片时段重叠，应自动避让到被重叠卡的前一空闲段，否则弹回拖动前位置；④ 模板视图预置朝九晚五上班族 / 自由职业 / 大学生等日程模板。纯胶水层 + CSS 改动，领域接口零改动。
- **grill 决策**：
  - **D1（抓手标识）**：卡片头部左侧新增 `.grip` 六点抓手 glyph（CSS 径向渐变），`opacity .45→.95` 悬停强化；`.head`/`.block.dragging` 分别 `cursor:grab/grabbing`。
  - **D2（防错位）**：拖拽吸附粒度由 1 小时改为 **5 分钟**（贴合既有「吸附到 5 分钟」规格，消除快速拖动时 ±30 分钟的视觉脱节）；`.block` 设 `transform-origin:top left`，使 `dragLift` 的放大/旋转不再以中心为基点把卡顶边缘与指针拉开；拖拽期间 `transition:none` 保证实时跟手；`mousemove` 中锚定 `drag.ns`（绝对坐标计算、无累计误差）。
  - **D3（松手避让）**：新增 `resolveDropCollision(ns,dur,bid,orig)`——落点与其它卡冲突时，取冲突卡最早的开始时刻 `cstart`，尝试落到 `[floor((cstart-dur)/5)*5, cstart]` 前空隙段；若该段 <0 或仍与其它卡重叠则**弹回拖动前 `orig`**。
  - **D4（模板预设）**：新增 `PRESETS`（上班族/自由职业/大学生，各含名称/起止/8 色色板/重复规则）；模板视图顶部渲染 `.preset` 卡片带（名称+描述+3 个区块 chip）；点击经 `confirm` 确认后替换当前模板结构（`applyPreset`，只影响未来未定形的日子）。
- **实现**（`planloop.html`）：CSS 新增 `.grip`、`.presets/.preset/.pname/.pdesc/.pchips/.chip` 与 `cursor`；胶水层新增 `PRESETS`、`grip` 头部标记、`mousemove` 5 分钟吸附 + `drag.ns`、`resolveDropCollision`、`endDrag` 避让落点、`renderTemplates` 预设带渲染 + `presetStrip` 应用 handler。
- **校验**：`node --check` 通过；`tests/domain.test.js` **51/0** 全绿（领域层零触碰）；CDP 合成拖拽事件实测——「短卡1」05:00 拖向 09:00 冲突→自动落到前一空闲段 **08:00**；再将 08:00 卡拖向 10:00 冲突（前段 09:00 被挡）→**弹回原 08:00**；抓手计数=卡片数、`head` cursor=grab；模板页渲染 3 张预设卡、点击「朝九晚五上班族」后模板列表由 1 扩充为 **5**（localStorage 同步 5）。截图存 `.screenshots/cr006_day.png`、`cr006_templates.png`。

## 变更请求 CR-007：5 套高级感颜色主题自由切换

- **2026-08-22** 用户要求做出 5 个可自由切换、有高级感的颜色主题。纯样式层 + 状态字段扩展，领域接口零改动（`tests/domain.test.js` 保持全绿）。
- **grill 决策**（需求收敛）：
  - **D1（形态）**：在既有「明暗」双态之外，引入「主题色」维度，二者正交叠加（5 色 × 明暗 = 10 种组合），保留下述明暗切换能力不冲突。
  - **D2（色板）**：5 套克制高级感配色，各含配套浅/深变体，用 CSS 变量覆盖实现，可完全随 `data-palette` 换肤——**海盐蓝**(默认)、**琥珀金**、**翡翠绿**、**暮紫**、**曜墨**。
  - **D3（交互）**：顶栏新增 `◈` 调色板按钮，弹出毛玻璃圆点选择器（5 彩点，当前配色描边高亮）；点击即应用并写入 `settings.palette`，随 JSON 导出/导入；点击外部或 Esc 关闭；保留原 `◐` 明暗切换。
- **实现**（`planloop.html`）：
  - CSS：新增 5 套 `body[data-palette="…"]`（浅色）与 `body[data-theme="dark"][data-palette="…"]`（深色）变量覆盖（重定义 `--bg/--bg-grad/--accent/--accent-2/--accent-soft/--ring`）；新增 `#paletteWrap/.palette/.ptitle/.pdot` 选择器样式（毛玻璃弹出、`mask:none` 规避顶栏渐变遮罩、`.pdot.on` 高亮环）。
  - 胶水层：`state.settings` 增 `palette:'salt'` 默认（含迁移兜底）；`applyTheme()` 改为同时写 `data-theme` 与 `data-palette`；新增 `PALETTES` 名称表、`setPalette()`、`syncPaletteDots()`、`paletteBtn`/`paletteMenu` 开合与点外/Esc 关闭。
- **校验**：JS 语法 `node --check` 通过；`tests/domain.test.js` **51/0** 全绿；CDP 无头 Chrome **真实点击路径**实测——点 `◈` 开菜单（5 彩点）、依次点选 5 色、再点 `◐` 切深色，逐项截图确认每套配色浅/深均正确换肤（翡翠绿浅、暮紫深、曜墨深等），底部 toast「已切换到『××』主题」正常；`localStorage['planloop.v2'].settings.palette/theme` 随切换同步（持久化经核实）。截图存 `.screenshots/cr007_{salt|amber|jade|violet|onyx}_{light|dark}.png`、`cr007_menu.png`。
- **遗留验证**：配色手感（对比度、明暗氛围）需用户本地浏览器实际体验确认一次。

## 变更请求 CR-008：调色板弹层遮挡修复 + 拖拽闪动抑制 + 首页 UI 升级

- **2026-08-22** 用户反馈三点：① 选主题的悬浮气泡（调色板菜单）被挡住；② 拖动时间块时不要所有卡片都闪一下，只拖动那张即可；③ 首页 UI 不佳——字体太小、时间轴未划分上午/下午/晚上，并结合爆款产品做优化。
- **grill 决策**：
  - **D1（弹层遮挡）**：根因是调色板菜单挂在 sticky 顶栏内部，被顶栏的 `mask-image`（顶部渐变遮罩）裁掉底部。改为把菜单**移出顶栏**、`position:fixed`（`z-index:120`，高于一切含导览层），展开时按按钮 `getBoundingClientRect` 定位并限制不超出视口。
  - **D2（拖拽闪动）**：根因是 `endDrag` 松手后调用 `renderDay()` 整页重建所有卡片、触发每张卡的 `blockIn` 入场动画。改为渲染期间给 `#timeline` 加 `.no-anim`（CSS `#timeline.no-anim .block{animation:none}`），重建完再移除，仅被拖卡位置更新、其余不重放入场。
  - **D3（首页 UI）**：结合苹果日历/滴答清单类爆款产品：① 提整体字号（body 14.5→15、副标题/状态/统计标签/时间刻度逐项上调）；② 时间轴按 **6-12 上午 / 12-18 下午 / 18-24 晚上** 渲染三段分区（极淡主题色带 + 左上角「上午/下午/晚上」标签，下午暖金、晚上暮紫）；③ 每张时间卡头部显示起止时段（`07:30–08:45` 时间 chip，契合爆款时间块习惯）；④ 统计卡数字加大、留白微调。
- **实现**（`planloop.html`）：CSS 调整 `.palette` 定位与新增 `#timeline.no-anim`、`.ttime`、`.zone{…}` 系列；HTML 把 `#paletteMenu` 移到顶栏外；胶水层 `paletteBtn.onclick` 加计算定位与视口夹持、`document click` 点外判断纳入菜单本身；`renderDay` 增加 zone 渲染与 `ttime` chip；`endDrag` 包 `no-anim`。
- **校验**：JS 语法 `node --check` 通过；`tests/domain.test.js` **51/0** 全绿（领域层零改动）；CDP 无头 Chrome 实测——`ZONE_DIAG`：zone 标签 ['上午','下午','晚上']、3 块均有 ttime；`MENU_DIAG`：菜单 open=true、top61/bottom115、`z-index:120`，截图确认 5 色完整可见未被遮挡；`Input.dispatchMouseEvent` 真实输入拖拽 `dragging:true`、块随动、结束后 `.no-anim` 已被移除且全程 `EXCEPTIONS[]`（无异常）。截图存 `.screenshots/cr008_day.png`、`cr008_menu.png`、`cr008_afterdrag.png`。
- **遗留验证**：分区色带与卡片叠加的美感、字号观感，需用户本地浏览器实际体验确认一次。

## 变更请求 CR-009：产品理念开场引导（两段式）

- **2026-08-26** 用户在用户旅程梳理后提出：产品引导不仅要教操作，还要传递产品哲学与设计理念，让用户从"会用"升到"认同"。
- **grill 收敛**：采纳"两段式"——新增独立理念开场覆盖层（3 张哲学卡：把决定提前 / 结构护住黄金时段 / 只影响未来），播放完再进入既有 5 步操作导览。顶栏「？」仍只重播操作导览。纯胶水层 + `settings.philoIntro` 标记，领域接口零改动。
- **PRD §4.10**、**SPEC §9** 成文。
- **实现**（`planloop.html` 胶水层 + 样式）：新增 `.philo-veil/.philo-card` 覆盖层样式；`PHILO_CARDS` 3 步引擎（单卡居中、进度点、下一步/开始使用/跳过）；`finishPhilo(continueTour)` 后按需衔接 `startTour()`；首次启动 `!philoIntro && !onboarded` 先播理念。
- **校验**：`node tests/domain.test.js` **66/0** 全绿（新增领域函数见 CR-010）；`--dump-dom` 确认 philo-card 与首卡文案渲染；playwright-core 驱动系统 Chrome 实测 13 例全过（首卡→卡2→卡3→开始使用→衔接导览→刷新不重播→持久化）。

## 变更请求 CR-010：心情 3 档 + 富文本复盘（Markdown）

- **2026-08-26** 用户在用户旅程梳理后提出：复盘除了心情"好中坏"，还支持富文本（可选）。采纳 Markdown 语法方案——长复盘有层级但保持纯源码字符串存储，契合单文件/导出干净。
- **grill 收敛**：
  - D1（心情）5 档改 3 档（好 🙂=3 / 中 😐=2 / 坏 😞=1），旧存档经 `normMood` 归并（4/5→3、3→2、1/2→1）。
  - D2（富文本）领域层新增可测纯函数 `mdToHtml(src)`（标题 1–3 / 无序 / 有序 / 加粗 / 行内代码 / 段落），**全部文本先 `mdEsc` 转义再套标签**，杜绝 XSS。复盘输入框下方加「预览/编辑」切换，存 Markdown 源码。
- **PRD §4.11**、**SPEC §10** 成文。
- **实现**：领域层新增 `normMood`、`mdToHtml`、`mdInline`、`mdEsc`（纯函数，导出进 `createDomain` 返回）；胶水层心情改 3 按钮、复盘富文本预览容器 `#reviewPreview` + `#revPreviewToggle` 切换；`renderReview` 用 `normMood` 归并老数据。
- **校验**：`tests/domain.test.js` 新增 normMood 6 例 + mdToHtml 8 例 → **66/0** 全绿；JS 语法通过；playwright-core 实测 12 例全过（3 心情按钮/存源码/预览 h1+ul+strong+code/切回编辑/XSS 不执行脚本）。

## 变更请求 CR-011：场景 2 记录分享（规划中，未实现）

## 变更请求 CR-012：Apple 风格全面重估（ui-ux-pro-max 驱动）

- **2026-08-27** 用户要求用 `ui-ux-pro-max` 技能把项目「完全重估」成 Apple 风格。
- **工具情况**：技能已加载，但其 `--design-system` 生成器依赖的 `design_system.py` 在本机技能包中缺失，自动生成不可用。转而调用技能可用的 `core.py` 检索引擎（product/style/color/typography/ux/icons）获取数据库推荐。
- **grill 决策（依据技能检索）**：
  - **产品类型**：Productivity Tool + Calendar & Scheduling App → 推荐 **Flat + Micro-interactions、Minimalism、Soft UI**，Dashboard 用 Drill-Down Analytics，配色「Clear hierarchy + functional colors」。
  - **风格**：**Bento Box Grid / Apple-style**（off-white `#F5F5F7`、text `#1D1D1F`、18-20px 圆角、soft shadows、hover scale 1.02）；Glassmorphism 用于顶栏/浮层；动效 Tier Subtle/Standard（`transform/opacity` only、150-250ms、`power1/power2.out`）。
  - **颜色**：回归 Apple 中性系统灰——浅色 `--bg:#f5f5f7`/`--card:#fff`/`--ink:#1d1d1f`，深色 `--bg:#000`/`--card:#1c1c1e`/`--ink:#f5f5f7`；强调 `--accent:#0071e3`（iOS 蓝，浅）/`#0a84ff`（深）；5 套 palette 降为**只覆盖 accent 系**、底恒为中性灰。
  - **排版**：系统字体栈 Apple 优先（`-apple-system, "SF Pro Display", "SF Pro Text", "Helvetica Neue", "PingFang SC"…`），数字 `tabular-nums`。
  - **交互/无障碍（ux 高优先规则）**：触控目标抬到 **38px**（icon/date/mini/mood 42px）、图标/按钮间距 ≥8px、hover 位移 <2px 或 scale、`--ease:cubic-bezier(.32,.72,.35,1)`（Apple 标准）、尊重 `prefers-reduced-motion`。
- **实现**（`planloop.html`，纯样式层重写整个 `<style>` 块，**DOM 与 JS 逻辑零改动**）：
  - 设计令牌重构：`--line-soft`、`--shadow-sm/md→shadow/shadow-lg` 语义化，圆角 `--r-sm/md/lg/xl`，统一 `--ease`。
  - 视觉刷新：背景去偏蓝回归 `#F5F5F7` 中性灰；卡片改 **白卡浮浅灰、弱描边 + 柔和双层阴影**（不再靠重边框）；iOS 分段控件胶囊化（`.nav`/`.seg` 选中白底 + 微影）；顶栏/浮层/palette/modal 毛玻璃统一 `saturate(180%) blur(20-22px)`；Bento 统计卡（数字 30px、顶部细渐变条取代左侧竖条）；当前时间红线加 `nowPulse` 呼吸；hover 统一 power2 微动效（translateY/scale + shadow-lg）。
  - 触控与可读性：`.iconbtn 34→38px`、`.datebtn 32→36px`、`.mini/mood/dow` 同步加大；`--muted-2` 新增用于滚动条 thumb 对比；字号与留白微调。
- **校验**：`tests/domain.test.js` **66/0** 全绿（领域零改动）；JS `node --check` 通过；CDP 无头 Chrome 实测——3 块渲染正常、分区（上午/下午/晚上）完好、切深色、切 amber 配色（`settings.palette` 持久化）、五页签逐页截图、全程 `EXCEPTIONS[]` 无 JS 错误、控制台无网络错误。截图存 `.screenshots/cr009_*`。
- **产物**：`design-system` 系统设计随本项目内嵌于 `.screenshots/` 外，token 语义记录见本条目；未额外生成设计令牌文件（保持单文件架构）。
- **遗留验证**：毛玻璃手感、hover/开关动效、深浅主题实际质感建议用户本地浏览器体验确认；分段控件 5 段在窄屏的横向拥挤感可后续评估（CR-012 已保留 `720px` 断点换行布局）。

## 变更请求 CR-013：新手指引导览优化（遮挡修复 + 模板优先）

- **2026-08-27** 用户反馈：① 新手指引时（顶栏导航换行成多行时）把原有按钮挡住，如截图；② 引导应从制定模板开始——先挑喜欢的作息模板，再调整模板，然后才介绍日程相关页。
- **grill 决策**：
  - **D1（遮挡根因）**：顶栏 `.topbar` 的 `mask-image` 是 `linear-gradient(to bottom,#000 84%,transparent)`，导航 `flex-wrap` 换行成多行时，第二行导航按钮正好落在渐隐区底部被视觉淡化，误判为「引导遮挡按钮」。改为 `linear-gradient(to bottom,#000 calc(100% - 14px),transparent)`，仅保留最后 14px 收尾渐隐，不再覆盖导航按钮。
  - **D2（步骤重排）**：操作导览 `TOUR_STEPS` 由 5 步扩为 6 步，**模板前置**：第 1 步选模板（`.preset`）→ 第 2 步调模板（`.addtpl`）→ 第 3 步「今日」主界面 → 第 4 步状态徽章 → 第 5 步复盘 → 第 6 步待办池。选中逻辑按用户原话「选模板→调模板→介绍日程页」收敛。
  - **D3（视图自动切换）**：每步新增 `view` 字段（`templates/day/review/backlog`），但在 `renderTour()` 中**此前未调用 `switchView(s.view)`**，导致模板步即便选中 `.preset`/`.addtpl`，若用户当前停在「今日」视图，渲染的模板卡处于隐藏态，高亮落点失真。修复：`renderTour()` 开头补 `if (s.view) switchView(s.view);`，使每步自动切到所属视图后再定位高亮。
- **实现**（`planloop.html`）：CSS 改 `.topbar` 一行 `mask-image`；胶水层重写 `TOUR_STEPS`（6 步 + 每步 `view`），`renderTour()` 首行加视图切换。领域接口零改动。
- **校验**：`tests/domain.test.js` **66/0** 全绿；CDP 无头 Chrome 实测 6 步逐步点击——每步 `view` 均正确切换（`view-templates`→`view-templates`→`view-day`→`view-day`→`view-review`→`view-backlog`），高亮框逐项落在预设卡/加新卡/导航按钮/状态徽章/复盘/待办池，nav 按钮高亮位于顶栏内、veil `clip-path` 正常挖空目标不被遮，全程无 JS 异常。

## 变更请求 CR-014：整体重设计（沉淀 + 轻量闭环，用户旅程驱动）

- **2026-08-27** 用户反馈"效果还是不好，连我自己都不想用"，要求认真研读 `USER_JOURNEY.md` 后把整个应用重新设计。
- **grill 根因诊断（依据用户旅程跨场景洞察）**：
  - **D1（无沉淀回报）**：旅程「场景2 痛点1」明确指出"记录没有沉淀：执行和心情散落在各天，看不到趋势，复盘只能靠记忆，记录价值没有兑现"。旧版只有今日进度环，**无完成率趋势 / 心情曲线 / 里程碑 / 累计数据**，用完没有回头的复利理由。
  - **D2（状态 4 态循环繁琐）**：原状态点按在 `点我→完成→部分→跳过` 间循环，违反旅程「场景2 痛点2」要"记录成本越低越容易坚持"的轻量诉求。改为**只保留 完成⇄未完成 2 态**为主交互（部分/跳过域层保留但不展示）。
  - **D3（闭环感弱、导航分散）**：今日被拆在 tab 外，结构/周/复盘/待办 五个平级入口、周与复盘分离且无统计。改为 **4 个对齐每日闭环的入口：今日(执行)→复盘(沉淀)→结构(调整)→待办(续)**，把原"周"视图的数据并入复盘页的"本周沉淀"。
  - **D4（正反馈被稀释）**：原每个块完成都触发满屏彩带，情绪麻木。改为**每块完成只做轻 pop**；彩带只留给"当日全部完成 / 连续 3·7 天里程碑"，首块完成给轻 toast——把正反馈用在刀刃上。
- **实现**（`planloop.html`，仅重写 `GLUE` + `<style>` + 结构，`CORE` 领域层逐字保留）：
  - 今日：`#todaycard` 紧凑问候 + 日期 + done/total 细进度条 + 连续天数 pill；时间块改为整行（左彩条 + 时间 + 块名 + 内联输入 + 右侧圆形勾选钮，一按即完成/取消即撤销）。
  - 复盘：原复盘（勾选 carry + 心情 3 档 + Markdown 笔记预览）之上，新增 **「本周，你在变好」沉淀区**——`statgrid` 里程碑 3 卡（连续坚持 / 本周完成过 / 累计完成块数）+ `#weekBars` 7 天完成率柱状（今日高亮）+ `#moodStrip` 最近 7 天心情带（😊😐😞）。均由新聚合函数 `statOf/streak/cumCompleted/renderInsights` 计算，`blocksOf` 沿用懒物化投影，历史未开过机的日子按模板投影计 0。
  - 导航：今日/复盘/结构/待办 4 tab + 图标；引导改为**模板优先的温暖 5 步**（欢迎→挑模板可即时套用→今日点一下完成→复盘收尾→开始）。
- **修复重写过程中引入的 2 个 bug**：① `.tab` 顶栏导航原先**没有绑定点击切换**（用户在冒烟中发现切视图失败），补 `switchView(b.getAttribute('data-view'))`；② `renderReview` 调用了重写时被误删的 `ensureDayBlocks` 辅助函数，导致复盘页抛 `Uncaught`、本周柱状不渲染，已补回。
- **校验**：`tests/domain.test.js` **66/0** 全绿（CORE 零改动）；JS `node --check` 通过；CDP 无头 Chrome 冒烟（修复了 `_smoke.js` 里 `pws.onmessage` 覆盖 CDP 路由导致挂起的隐患，改用 `addEventListener`）——今日 2 块渲染、完成 1/2 且再点可撤销回 0/2、复盘视图 `bars:7 / mood:7 / 里程碑卡` 渲染、结构 `presets:3 / tpls:2`、模板弹窗开启、待办空态、深浅主题切换，全程 `exceptions:[]` 无 JS 异常。截图存 `.screenshots/cr014_*`。
- **决策依据**：严格落到 `USER_JOURNEY.md` 的「场景2 日常反复使用」为基本盘——先补齐沉淀 / 统计 / 轻量心情（不碰架构），再待后续评估分享入口（CR-011）。

## 变更请求 CR-015：轻量人生节奏转型（产品重新定位）

- **2026-09-02~09-03** 用户在探讨产品方向时提出新观点："人生不要过度规划可减少内耗，但完全不规划也不行"，追问 PlanLoop 是否该改成"极简个人日程管理"。经发散讨论 + 结构化提问（AskUserQuestion），定位收敛为**轻量人生节奏工具**。
- **grill (grill-with-docs) 九连决（Q1–Q9，2026-09-03）**：
  - **Q1(c)** 首要痛点是**操作负担**（"填计划本身费劲"）→ 口号「填=选，不是写」。
  - **Q2(b)** **复盘彻底弱化为「回望」**：只保留完成态 + 正向沉淀，砍掉"计划 vs 实际"偏差问责。
  - **Q3(a)** 未完成 → **一键顺延，可换时段**，零必填零问责。
  - **Q4(b+自然)** **撤销"精确到钟点的时间块"**，改**自然时段**：上午/午间/下午/晚上/午夜。
  - **Q5(a)** 时段**带可改默认钟点窗口**，钟点仅作弱提示"当前该看哪段"，不逼守点；改时段=只改顺序。
  - **Q6(c)** 候选来源：**昨日+高频复用 ＋ 固定钉**并重。
  - **Q7(a)** **低频优先去重**：钉恒排最前→最近/高频紧随，同内容只占一格。
  - **Q8(a)** **遗留待办池并入候选池**：顺延项自然成为未来某时段候选之一，不设独立概念。
  - **Q9(a)** **只保留「只影响未来」；砍掉「每日快照/惰性物化」**：每天实时反映当前结构，过去只留记录。
- **定位一句话**：减内耗的关键 = 把"承诺"做轻、把"结构"做稳——结构由纪律软化为节律，填靠"选"不靠"写"，没做完无咎顺延，每天只留下"实际怎么过"的轻回声望。
- **对既有实现的影响**：与 CR-014 沉淀区(正向/统计/轻量)同向，沉淀可保留；**拆除** 24h 钟点时间轴/拖拽(CR-003/005/006)、独立遗留待办池、偏差复盘、每日快照/惰性物化机制。
- **文档固化（2026-09-03，先文档后代码）**：
  - `CONTEXT.md` **整体重写**为转向后术语表（新增：时段结构/时段/候选池/倾向/今日填充/一键顺延/执行记录/回望/完成·未完成；新增「已废弃术语」节标注快照/惰性物化/待办池/偏差复盘/部分·跳过）。
  - `PRD.md` 追加 **§8 变更·轻量人生节奏转型(CR-015)**：定位、Q1–Q9 决策表、目标、转向后功能需求、范围边界、完成判据；声明取代 §1–§7 中"硬时间块承诺"为前提的内容。
  - `SPEC.md` 追加 **§11 CR-015 实现规格**：v3 数据模型(`slots/pins/dayFills/carried/settings`)、领域新接口(`candidatesFor/recentFor/pickFill/completeSlot/carry/lookbackOf`)、废弃接口清单、v2→v3 迁移、胶水层改动、拆除清单、8 步 TDD 任务。
- **流程状态**：grill 收敛 → CONTEXT/PRD/SPEC 文档已固化。**尚未进入实现**（按项目硬规矩，实现阶段单独开工，动代码前需用户对本 Spec §11 评审确认）。

## 变更请求 CR-016：灵感卡片（候选池轻量新来源）

- **2026-09-03** 用户在演示版实操中反馈：日常被"上班/下班/刷手机"充斥，"写候选项都挺迷茫，不知道还能干啥"。这正是候选池三类来源（钉/最近·高频/顺延）都源于用户过往历史、只能互相喂养的盲区——工具成了空洞生活的镜子。
- **grill 结论（就在对话中锁定，用户确认"干吧"）**：
  - **Q1** 候选池新增第四类来源 **灵感卡片**：按自然时段（上午/午间/下午/晚上/午夜）内置极少量小巧候选，排在同段"钉/顺延/最近"之后作为兜底逃逸口。措辞克制、不似"处方"。
  - **Q2** **零新领域概念、零新增状态**：卡片是运行时内置常量（`IDEA`/`IDEA_ANY`），不写入 saved state；**不统计、不比较、不个性化、不建排行榜**——选中才走 `pickFill` 变成普通项，天然不额外累积"任务处方"指标。
  - **Q3** 未选中的灵感卡**不进入顺延管网**（`carried` 只收真实选中的未完成项）；被选中的灵感项如同普通项，可完成/撤销/顺延。
  - **Q4** 顺序与优先级：钉 > 顺延 > 最近·高频 > 灵感；真实候选不足 `max` 时才补灵感，避免挤占真实候选。
- **实现（TDD）**：`candidatesFor` 追加 `kind:'idea'` 来源；新增 `IDEA`/`IDEA_ANY`/`ideaFor`；候选徽标文案加「灵感」，芯片渲染加 `idea-tag`；CSS 虚线与弱化样式（`cand.idea`）。
- **校验**：`tests/domain.test.js` **16/0** 全绿（含新增 1 条灵感卡片来源测试）；CDP 无头冒烟 SMOKE OK、`exceptions:[]`；候选框实拍确认灵感卡渲染（`_smoke_tmp/idea-candidate.png`）。另顺带修复此前演示模式 CSS 中 `textarea` 与心情按钮间距过近问题。
- **决策依据**：严格贴合"结构做稳、承诺做轻"；灵感卡是规避"又被打造成药方"内耗风险的最低代价设计——无存储、无推断、无统计。